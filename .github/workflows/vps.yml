name: VPS SSHX 24/7

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  apolinario:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: MGGJJG Cloud 24/7
        run: curl -sSf https://sshx.io/get | sh -s run
